def if_and_else_condition(condition_variable):
    print('[Beispiel] if-Anweisung mit einer Else-Anweisung')
    
    # Wenn die Bedingung zutrifft, dann wird das eingerückte ausgeführt
    if (condition_variable == True):
        print('Bedingung erfüllt')

    # Andernfalls das eingerückte unter dem else
    else:
        print('Bedingung nicht erfüllt')

    return